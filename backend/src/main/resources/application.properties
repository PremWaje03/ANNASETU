spring.application.name=annasetu-backend
server.port=8081

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/annasetu_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Prem7276
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT
app.jwt.secret=VGhpc0lzQVNlY3VyZUFubmFzZXR1SldUU2VjcmV0S2V5Rm9yUHJvZHVjdGlvbjEyMw==
app.jwt.expiration-ms=86400000

# WebSocket
app.websocket.allowed-origins=http://localhost:5173,http://localhost:3000
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Uploads
app.upload.dir=uploads
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB

# Expiry Alert Scheduler
app.expiry.scheduler-ms=60000

# Logging
logging.level.org.springframework.security=INFO
